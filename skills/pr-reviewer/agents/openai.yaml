interface:
  display_name: "PR Reviewer"
  short_description: "Gate tested changes with security and approval policy"
  default_prompt: "Use $pr-reviewer to independently review and gate this tested candidate."
