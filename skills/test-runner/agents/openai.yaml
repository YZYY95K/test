interface:
  display_name: "Test Runner"
  short_description: "Verify candidates with fail-closed sanitized evidence"
  default_prompt: "Use $test-runner to verify this candidate in isolation and emit digest-bound sanitized evidence."
